import asyncio
import asyncio
import mlb
import embeds
import cache
async def watch_home_runs(bot, channel):
    print("Watching for home runs...")

    while True:
        try:
            # Placeholder until we add the live HR detection logic
            print("Checking MLB API...")

        except Exception as e:
            print(f"Error: {e}")

        await asyncio.sleep(15)